// Objeto Math

let resultado;

resultado = Math.PI;
resultado = Math.round(2.5);
resultado = Math.ceil(2.9); // ceil redondear hacia arriba
resultado = Math.floor(2.1); // floor redondear hacia abajo
resultado = Math.sqrt(144); // raiz cuadrada
resultado = Math.abs(-200);
resultado = Math.min(-20, 2, 4, 3);
resultado = Math.max(-20, 2, 4, 3);
resultado = Math.random();
resultado = Math.floor( Math.random() * 30);

console.log(resultado);
